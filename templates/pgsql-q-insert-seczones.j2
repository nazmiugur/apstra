INSERT INTO {{ sz_tablename }} (name,blueprint_id,remote_id,status)
VALUES
{% for row in seczones %}
  {% if not loop.last %}
  ({{ row }},'{{ datetime_of_pb }}'),
  {% endif %}
  {% if loop.last %}
  ({{ row }},'{{ datetime_of_pb }}');
  {% endif %}
{% endfor %}
